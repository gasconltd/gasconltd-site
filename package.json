{
  "name": "gasconltd-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static HTML site — Oryx requires build/build:azure scripts for Azure Static Web Apps",
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "build:azure": "npm run build"
  }
}
